🎵 Record Collection App Setup

✅ STEPS:
1. Unzip this folder somewhere like: D:\record-collection-app
2. Open PowerShell inside the folder
3. Run:

   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   .\setup.ps1

That’s it — it will install dependencies and auto-run frontend + backend servers.

🛑 If you get a 'file not found' error, recheck that you fully unzipped the folder.
